info:
  name: Удаление комментария
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{URL}}api/tickets/1/comments/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

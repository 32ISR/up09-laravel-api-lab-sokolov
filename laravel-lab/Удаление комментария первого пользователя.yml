info:
  name: Удаление комментария первого пользователя
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{URL}}api/tickets/2/comments/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

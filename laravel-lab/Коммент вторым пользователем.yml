info:
  name: Коммент вторым пользователем
  type: http
  seq: 11

http:
  method: POST
  url: "{{URL}}/api/tickets/1/comments"
  body:
    type: json
    data: |-
      {
        "body": "Проверьте логи в storage/logs/laravel.log!!!!!!!!!!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

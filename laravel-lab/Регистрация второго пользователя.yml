info:
  name: Регистрация второго пользователя
  type: http
  seq: 9

http:
  method: POST
  url: "{{URL}}api/register"
  body:
    type: json
    data: |-
      {
          "name": "Вассилий Петрович",
          "email": "vasily@example.com",
          "password": "password123",
          "password_confirmation": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

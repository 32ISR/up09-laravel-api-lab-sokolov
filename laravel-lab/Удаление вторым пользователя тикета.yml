info:
  name: Удаление вторым пользователя тикета
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{URL}}api/tickets/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
